<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="steven.springboot.example.mybatisfirst.UserQuery.TestXml">
    <select id="findAll" resultType="steven.springboot.example.mybatisfirst.UserClass.UserClass">
        select id, username, password, name, gender, phone, job, salary, entry_date, image, create_time, update_time
        from empx
    </select>
<!--    <delete id="deleteById">-->
<!--        delete from empx where id=#{id}-->
<!--    </delete>-->
<!--    <insert id="insert">-->
<!--        insert into empx(username, password, name, gender, phone, job, salary, entry_date, image, create_time,-->
<!--        update_time) values(#{username}, #{password}, #{name}, #{gender}, #{phone}, #{job}, #{salary}, #{entry_date},-->
<!--        #{image}, #{create_time}, #{update_time})-->
<!--    </insert>-->
<!--    <update id="update">-->
<!--        update empx set username=#{username}, password=#{password}, name=#{name}, gender=#{gender}, phone=#{phone},-->
<!--        job=#{job}, salary=#{salary}, entry_date=#{entry_date}, image=#{image}, create_time=#{create_time},-->
<!--        update_time=#{update_time} where id=#{id}-->
<!--    </update>-->
<!--    <select id="queryUser" resultType="steven.springboot.example.mybatisfirst.UserClass.UserClass">-->
<!--        select * from empx where username != #{username}-->
<!--    </select>-->
    <!--    <select id="queryGroup" resultType="steven.springboot.example.mybatisfirst.UserClass.UserClass">-->
    <!--        select username,job, avg(salary) avg_salary,entry_date,create_time from empx where entry_date <= '2023-01-01'-->
    <!--        group by username,job, salary,entry_date,create_time-->
    <!--        having avg_salary > 8000-->
    <!--        order by avg_salary desc,entry_date;-->
    <!--    </select>-->
</mapper>